<?php
/**
 * Paystack keys. Copy to paystack.php and fill in.
 * Use TEST keys (sk_test_/pk_test_) until go-live, then swap for LIVE keys.
 * The webhook secret = your SECRET key (Paystack signs webhooks with it).
 */
return [
    'public_key' => 'pk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx',
    'secret_key' => 'sk_test_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx',
    'currency'   => 'NGN',
    // Buyer is returned here after paying; we verify-by-reference on arrival.
    'callback_path' => '/checkout/callback',
    'webhook_path'  => '/payment/webhook',
];
